{
  "name": "workforce-management-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "generate-resume": "node tools/generate_resume_pdf.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.9.0",
    "jsonwebtoken": "^9.0.0"
  }
  ,"devDependencies": {
    "puppeteer": "^20.0.0"
  }
}
