{
  "name": "workforce-management-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "generate-resume": "node tools/generate_resume_pdf.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0"
  }
  ,"devDependencies": {
    "puppeteer": "^20.0.0"
  }
}
